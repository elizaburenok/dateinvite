/**
 * Константы контракта без единой зависимости.
 *
 * Отдельный модуль не ради красоты: фронтенды берут отсюда числа, а из contract.ts —
 * только типы. Если бы константы жили рядом со схемами, любой импорт значения тащил
 * бы в бандл Mini App весь zod (≈90 КБ) ради двух целых чисел.
 */

/** Конверт строго на 3–5 мест — §4, приёмочный критерий §12. */
export const ENVELOPE_MIN_PLACES = 3;
export const ENVELOPE_MAX_PLACES = 5;

/** initData считается протухшей через сутки — §3, асимметрия авторизации. */
export const INIT_DATA_MAX_AGE_SEC = 24 * 60 * 60;
